# ══════════════════════════════════════════
#  .env.example — copier en .env et remplir
#  Ne jamais committer le .env réel !
# ══════════════════════════════════════════

# PostgreSQL
POSTGRES_DB=monapp_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me_in_prod

# Spring Boot (optionnel, remplace les valeurs dans application.properties)
# SPRING_PROFILES_ACTIVE=dev
